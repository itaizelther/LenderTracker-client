import { makeStyles } from "@rneui/themed";

const useStyles = makeStyles(() => ({
  container: {
    flex: 1,
    alignItems: "center",
    marginTop: 70,
    width: "85%",
  },
  header: {
    fontSize: 40,
  },
}));

export default useStyles;
