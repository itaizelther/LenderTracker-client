import { makeStyles } from "@rneui/themed";

const useStyles = makeStyles(() => ({
  container: {
    paddingRight: 50,
  },
  content: {
    fontSize: 15,
  },
  dynamicContent: {
    fontWeight: "bold",
  },
}));

export default useStyles;
